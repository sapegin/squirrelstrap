/* Author: {%= author_name %}, {%= author_url %}, {%= grunt.template.today('yyyy') %} */

/*global utils:false */
;(function ($) {
	'use strict';

	utils.initComponents({
		pony: function(elem) {
			// $(elem)
		}
	});

}(jQuery));
